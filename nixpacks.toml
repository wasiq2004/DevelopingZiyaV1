# Nixpacks configuration for Railway deployment
# This installs ffmpeg as a system dependency for audio conversion

[phases.setup]
nixPkgs = ["nodejs", "ffmpeg"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
